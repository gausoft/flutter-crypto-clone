import 'package:flutter/material.dart';

class FileEncryptionScreen extends StatelessWidget {
  const FileEncryptionScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}